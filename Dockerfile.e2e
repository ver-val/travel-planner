# syntax=docker/dockerfile:1.7

FROM node:20-alpine AS deps
WORKDIR /app
COPY package*.json ./
RUN npm ci

FROM deps AS test
COPY . .
RUN npm run build
ENV NODE_ENV=test
CMD ["sh", "-c", "npm run migration:run && npm run test:e2e"]
